<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs Write</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="write-page">
    <div class="container-fluid min-vh-100 text-center mb-4 d-flex justify-content-center py-5">
        <div class="card" style="width: 40rem;">
            <div class="card-body position-relative d-flex flex-column">
                <h1>
                <%= heading %>
                </h1>

                <% if (locals.post) { %>
                <form method="post" action="/post/<%= post.id %>/edit" class="d-flex flex-column flex-grow-1">
                        <div class="mb-3">
                            <input type="text" name ="title" class="form-control" placeholder="Title" value="<%=post.title %>" required>
                        </div>
                        <div class="mb-3">
                            <input class="form-control" type="text" name="author" placeholder="Author" value="<%=post.author %>" required >
                        </div>
                        <div class="mb-3 flex-grow-1 d-flex flex-column">
                            <textarea class="form-control flex-grow-1" name="content" placeholder="Content" required ><%=post.content %></textarea>
                        </div>                        
                        <button class="btn btn-success full-width mb-3" type="submit"> 
                            <%= submit %>
                        </button>
                    </form>
                    <form action="/post/<%= post.id %>/delete" method="post" >
                    <button type="submit" class="btn btn-danger w-100">Delete</button>
                    </form>
                <% } else {%>
                    <form method="post" action="/new" class="d-flex flex-column flex-grow-1">
                        <div class="mb-3">
                            <input type="text" name ="title" class="form-control" placeholder="Title" required>
                        </div>
                        <div class="mb-3">
                            <input class="form-control" type="text" name="author" placeholder="Author" required >
                        </div>
                        <div class="mb-3 flex-grow-1 d-flex flex-column">
                            <textarea class="form-control flex-grow-1" name="content" placeholder="Content" required ></textarea>
                        </div>                        
                        <button class="btn btn-success full-width mb-3" type="submit"> 
                            <%= submit %>
                        </button>
                    </form>
                    <form action="/post/-1/delete" method="post" >
                    <button type="submit" class="btn btn-danger w-100">Delete it</button>
                    </form>                    
                <% } %>
            </div>
        </div>
    </div>
    <div class="container-fluid footer bg-dark text-white text-center py-3 align-self-end">
        <p>
            &copy; 2025 Blogs Page. All rights reserved.
        </p>
    </div>
</body>
